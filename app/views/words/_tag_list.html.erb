<h2><%= t('tag_list') %></h2>
<ul>
  <% Word.active_tags.each do |tag| %>
    <li><%= link_to tag, word_tag_path(tag.name) %></li>
  <% end %>
</ul>
<%= link_to t('more'), tags_index_path, class: 'pull-right' %>
