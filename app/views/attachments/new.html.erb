<% content_for(:title) { "New Attachment" } %>

<div class="page-header mb-4">
  <h1 class="h2">New Attachment</h1>
</div>

<%= render 'form' %>

<%= link_to 'Back', attachments_path, class: 'btn btn-secondary' %>
